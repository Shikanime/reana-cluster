apiVersion: v1
kind: Service
metadata:
  name: {{ include "reanaui.fullname" . }}
  labels:
{{ include "reanaui.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: ui
      port: {{ .Values.service.port }}
      targetPort: ui
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ include "reanaui.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
