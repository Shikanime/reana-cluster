apiVersion: configuration.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/name: reana-cephfs
  app.kubernetes.io/version: stable
  app.kubernetes.io/component: ui
bases:
  - ../bases
  - ../../bases
patchesStrategicMerge:
  - reana-server/deploy-patch.yaml
  - reana-workflow-controller/deploy-patch.yaml
resources:
  - reana-cephfs-storageclass.yaml
  - reana-ingress.yaml
